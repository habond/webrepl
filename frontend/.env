# Frontend Environment Variables
# Vite exposes variables prefixed with VITE_

# Session refresh interval in milliseconds
VITE_SESSION_REFRESH_INTERVAL=30000

# Backend port for display purposes (actual routing via nginx proxy)
VITE_BACKEND_PORT=8000